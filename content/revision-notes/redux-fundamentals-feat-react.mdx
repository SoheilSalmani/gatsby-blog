---
title: Redux Fundamentals (feat. React)
resourceId: https://frontendmasters.com/courses/redux-fundamentals/
stoppedAt: Setting initialState
---

## Redux Without React

### Redux API's & Compose

```js nu fp=index.js
import {
  createStore,
  compose,
  applyMiddleware,
  bindActionCreators,
} from "redux"

const makeLouder = string => string.toUpperCase()
const repeatThreeTimes = string => string.repeat(3)
const embolden = string => string.bold()

const result = compose(embolden, repeatThreeTimes, makeLouder)
console.log(result("hello"))
```

### Redux Stores & Reducers

<Figure src="/media/reducer-image-explanation.png">
  Fancy explanation of what a reducer is.
</Figure>

```js nu fp=index.js
import {
  createStore,
  compose,
  applyMiddleware,
  bindActionCreators,
} from "redux"

const initialState = { value: 0 }

const incrementAction = { type: "INCREMENT", payload: 5 }

const reducer = (state, action) => {
  return state
}

const store = createStore(reducer)
console.log(store)
```

### Redux Stores & Dispatch

<Figure src="/media/reducer-image-explanation.png">
  Fancy explanation of what a reducer is.
</Figure>

```js nu fp=index.js hl=10,12,15..17
import {
  createStore,
  compose,
  applyMiddleware,
  bindActionCreators,
} from "redux"

const initialState = { value: 0 }

const INCREMENT = "INCREMENT"

const incrementAction = { type: INCREMENT }

const reducer = (state, action) => {
  if (action.type === INCREMENT) {
    return { value: state.value + 1 }
  }
}

const store = createStore(reducer)
console.log(store)
```

### Action Creators

```js nu fp=index.js hl=11,15,22..24
import {
  createStore,
  compose,
  applyMiddleware,
  bindActionCreators,
} from "redux"

const initialState = { value: 0 }

const INCREMENT = "INCREMENT"
const ADD = "ADD"

const incrementAction = { type: INCREMENT }

const add = amount => ({ type: ADD, payload: amount })

const reducer = (state, action) => {
  if (action.type === INCREMENT) {
    const value = state.value + 1
    return { value }
  }
  if (action.type === ADD) {
    return { value: state.value + action.payload }
  }
}

const store = createStore(reducer)
console.log(store)
```

### Setting initialState

### Some Rules for Reducers

### Subscribe & Binding Action Creators

### Combine Reducers

### Enhancers

### Enhancers Exercise & Solution

### Middleware

### Middleware Exercise & Solution

## Hooking It Up With React

### Connecting Redux into React

### Redux Dev Tools

### Connecting State Hooks to the Store

### Connecting Increment & Decrement

### Dispatching from Forms Exercise & Solution

### Binding Actions

## Connecting Redux to React

### Connect API vs Hooks

### mapStateToProps & Connect API

### mapDispatchToProps

### Connect API & mapDispatchToProps Exercise & Solution

### Updating Item Data

### Updating Item Data Exercise & Solution

### Deriving Data

## Selectors and Reselect

### Computing Data with Reselect

### Computing Data Exercise & Solution

## Immer

### Mutable State with Immer

### Immer Exercise & Solution

## Redux Toolkit

### Redux Toolkit & State Slices

### Creating a Slice of State

### Create a Slice Exercise & Solution

### Slice Actions & Creating Actions

### Extra Reducers

## Asynchronous Actions

### Async Thunks with React Toolkit

### Async Thunk Data Fetch Exercise & Solution

## Wrapping Up

### Wrapping Up
